version: "3.8"

services:
  postgres:
    image: postgres:16
    container_name: aif-postgres
    environment:
      POSTGRES_DB: aif
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app1234
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
